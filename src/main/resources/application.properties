spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/test310?serverTimezone=Asia/Taipei&characterEncoding=utf-8
spring.datasource.username=root
spring.datasource.password=Aa890724

spring.jackson.time-zone=GMT+8
spring.jackson.date-format=yyyy-MM-dd 

# 檔案上傳大小限制
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# JPA與Hibernate
# 自動更新資料庫結構 (update 表示如果表不存在會自動建立，欄位改變會更新)
spring.jpa.hibernate.ddl-auto=update
# 在Console顯示執行的SQL語法
spring.jpa.show-sql=false
# 格式化顯示的SQL
spring.jpa.properties.hibernate.format_sql=true

# Hibernate 批次寫入優化設定 (讓 saveAll 真的變成 Batch Insert)
spring.jpa.properties.hibernate.jdbc.batch_size=50
spring.jpa.properties.hibernate.order_inserts=true